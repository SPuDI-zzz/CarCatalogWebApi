import { Typography } from 'antd';
import React from 'react';

const Home = () => {
    return (
        <Typography.Title level={1}>Главная страница</Typography.Title>      
    );
};

export default Home;
